<% title "Templates" %>

<table>
  <tr>
    <th>Name</th>
    <th>User</th>
    <th>Description</th>
  </tr>
  <% for template in @templates %>
    <tr>
      <td><%=h template.name %></td>
      <td><%=h template.user_id %></td>
      <td><%=h template.description %></td>
      <td><%= link_to "Show", template %></td>
      <td><%= link_to "Edit", edit_template_path(template) %></td>
      <td><%= link_to "Destroy", template, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Template", new_template_path %></p>
