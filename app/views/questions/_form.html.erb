<%= error_messages_for :question %>
<% form_for @question do |f| %>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.label :survey_id %><br />
    <%= f.text_field :survey_id %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
